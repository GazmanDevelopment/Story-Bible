# Dev/test-only dependencies, on top of requirements.txt.
-r requirements.txt
pytest==9.1.1
# httpx is a runtime dependency now (requirements.txt, #22); still needed
# here too, for fastapi.testclient.TestClient - already covered by -r above.
playwright==1.63.0   # tests/test_ui.py - run `playwright install chromium` once after installing
packaging==26.3      # scripts/generate_third_party_notices.py - real PEP 508 marker/extra evaluation
